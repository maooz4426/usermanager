get:
  operationId: getUser
  description: user取得
  parameters:
    - $ref: "../components/UserUUID.yml"
  responses:
    200:
      content:
        application/json:
          schema:
            $ref: "../components/User.yml#/User"

